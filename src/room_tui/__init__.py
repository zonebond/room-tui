"""room-tui — local TUI workbench."""

# Bump when packaging/isolation/skills install path changes so Win installs
# can tell old room.exe from a fresh build (room --version / room doctor).
__version__ = "0.1.6"
